a = (1,45, 342, 3424 , False, "Jordan", "Corvette") # What a tuple looks like

# a = (1) # Will consider the value inside the brackets, and print the type of 1

# a = (1,) # Once added a ',' python will understand what you are trying to find the type of

print(type(a)) # Print if the type of a is tuple or so on so